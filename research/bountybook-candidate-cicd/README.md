# BountyBook candidate: CI/CD platform comparison

Candidate artifact for job `d47a42b6-8203-4bc7-8bbf-c22ef912c232` ($7 USDC).

The JSON is intentionally conservative: `free_tier_minutes` is `0` where a
provider uses credits or has no hosted free-minute allowance, and pricing text
directs the verifier to official plan terms instead of inventing a current
number. Sources are included in the optional top-level `sources` array.

This artifact is prepared only. Claiming or submitting the job requires a
separate exact Agent OS approval and guarded external effect.
