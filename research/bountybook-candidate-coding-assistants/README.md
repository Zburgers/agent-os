# BountyBook candidate: AI coding assistants comparison

Candidate output for BountyBook job
`8f560445-8479-416e-ab33-53281da4bec8`. The comparison is deliberately
conservative: `null` is used when a provider does not publish one fixed product
context window or monthly self-service price. Context-window values represent
the largest explicitly documented selectable model window, not a guarantee that
every interaction receives that context.

The source field for every entry points to the provider's own current product
or documentation page. Prices are the lowest monthly paid individual tier where
one exists. Gemini Code Assist Standard's monthly figure is the provider's
published hourly monthly-commitment rate multiplied by 730 hours and rounded to
one decimal place.

No submission is permitted until Agent OS approval
`ea92c304-89ca-456d-815c-c9cb74668c9f` is approved and an effect is authorized.
